import java.util.Arrays;

public class Buildin {

    char + int = (char)(6+'a');
    
    ch=str.charAt(4);
    str.compareTo(str2); // give + or - value
    str.compareToIgnoreCase(str2);
    str.equals(str2);

    s1 == s2;
    s1.equals(s3);

    boolean result = Arrays.equals(str1charArray, str2charArray);
    boolean result = str1charArray.equals(str2charArray);
    System.out.println(result);

    // str.length(); 
    str=str.concat(" is immutable so assign it explicitly");
    str.contains("value");

    str.endsWith("valule");
    str.startsWith("valule");

    Character.toUpperCase(ch1);
    Character.toLowerCase(ch1);
    // str.isEmpty();
    str.indexOf("value");

    str.replace('a','e');
    str.replaceAll('a','e');
    str.replaceFirst("a","e");

    str.startsWith(prefix, 0);
    
    str.substring(from,to);
    
    String s1="hello";  
    char[] ch=s1.toCharArray();  

    int value=30;  
    String st = String.valueOf(value);  

    str=str.trim();  //space 
    str.split("t", 0);


    str.equalsIgnoreCase(str1);

    String[] words = s.split("\\s+");      // split("\\s+") will split the string into string of array with separator as space or multiple spaces.

     char charArray[] = inputString.toCharArray();

     StringBuilder sb = new StringBuilder(s);
            char ch = sb.charAt(0);
            sb.deleteCharAt(0);
}
